osx_image: xcode8
language: objective-c

script:
- xcodebuild -project EZSwiftExtensions.xcworkspace -scheme EZSwiftExtensions-iOS -sdk iphonesimulator

before_install:
  - brew install swiftlint

before_script:
  - swiftlint
