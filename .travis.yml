osx_image: xcode8
language: objective-c

script:
- xcodebuild -project EZSwiftExtensionsExample.xcodeproj -scheme EZSwiftExtensions-iOS -sdk iphonesimulator
- xcodebuild -project EZSwiftExtensionsExample.xcodeproj -scheme EZSwiftExtensions-tvOS -sdk iphonesimulator

before_install:
  - brew install swiftlint

before_script:
  - swiftlint
  
after_success:
  - bash <(curl -s https://codecov.io/bash)
