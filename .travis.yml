osx_image: xcode8
language: objective-c

script:
- xcodebuild -project EZSwiftExtensions.xcodeproj -scheme EZSwiftExtensions-iOS -sdk iphonesimulator
- xcodebuild -project EZSwiftExtensions.xcodeproj -list -scheme EZSwiftExtensions-tvOS -sdk appletvsimulator

before_install:
  - brew install swiftlint

before_script:
  - swiftlint
  
after_success:
  - bash <(curl -s https://codecov.io/bash)
